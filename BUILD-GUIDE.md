# 🚀 AI Forge Studio - دليل البناء والتطوير

## 📋 المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [المتطلبات](#المتطلبات)
3. [التثبيت](#التثبيت)
4. [بناء تطبيق سطح المكتب](#بناء-تطبيق-سطح-المكتب)
5. [نشر PWA](#نشر-pwa)
6. [تطوير ومساهمة](#تطوير-ومساهمة)
7. [الميزات](#الميزات)

---

## 🌟 نظرة عامة

**AI Forge Studio** هو بيئة تطوير احترافية للذكاء الاصطناعي مع دعم كامل لتسريع GPU:
- ✅ واجهة مستخدم عصرية بتصميم Cyberpunk
- ✅ دعم كامل للغة العربية والإنجليزية
- ✅ تطبيق سطح مكتب (Electron)
- ✅ تطبيق ويب تقدمي (PWA)
- ✅ دعم CUDA، TensorRT، Vulkan

---

## 📦 المتطلبات

### للتطوير:
```bash
- Node.js 18+ و npm/yarn
- Git
- محرر نصوص (VS Code موصى به)
```

### لبناء تطبيق سطح المكتب:
```bash
- Electron 28+
- electron-builder
```

### لتشغيل التطبيق الكامل:
```bash
- NVIDIA GPU مع CUDA 11.8+
- CUDA Toolkit
- TensorRT 10.x
- Vulkan SDK
- 16GB RAM (موصى بـ 32GB)
```

---

## 🔧 التثبيت

### 1. استنساخ المستودع:
```bash
git clone https://github.com/QAZ83/ai-forge-studio-site.git
cd ai-forge-studio-site
```

### 2. تثبيت التبعيات:
```bash
npm install
```

### 3. تشغيل النسخة الإلكترونية:
```bash
# افتح index.html مباشرة في المتصفح
# أو استخدم خادم محلي:
npx http-server . -p 8080
```

---

## 🖥️ بناء تطبيق سطح المكتب

### تشغيل في وضع التطوير:
```bash
npm run dev
```

### بناء للأنظمة المختلفة:

#### Windows:
```bash
npm run build:win
```
**الناتج**: `dist/AI-Forge-Studio-1.0.0-Setup.exe` (~250 MB)

#### macOS:
```bash
npm run build:mac
```
**الناتج**: `dist/AI-Forge-Studio-1.0.0.dmg` (~280 MB)

#### Linux:
```bash
npm run build:linux
```
**الناتج**:
- `dist/AI-Forge-Studio-1.0.0.AppImage`
- `dist/AI-Forge-Studio-1.0.0.deb`
- `dist/AI-Forge-Studio-1.0.0.rpm`

#### بناء لجميع الأنظمة:
```bash
npm run build:all
```

---

## 🌐 نشر PWA

### 1. تفعيل Service Worker:
التطبيق يدعم PWA بشكل تلقائي! فقط قم بنشره على خادم HTTPS.

### 2. النشر على GitHub Pages:
```bash
git add .
git commit -m "🚀 Deploy PWA"
git push origin main
```

### 3. النشر على Cloudflare Pages:
```bash
# في لوحة تحكم Cloudflare:
1. اربط المستودع
2. اختر branch: main
3. Build command: (اتركه فارغاً)
4. Output directory: /
5. انشر!
```

### 4. اختبار PWA محلياً:
```bash
# استخدم خادم HTTPS محلي:
npx http-server . -S -p 8080
```

---

## 👨‍💻 تطوير ومساهمة

### بنية المشروع:
```
ai-forge-studio-site/
├── index.html              # الصفحة الرئيسية
├── dashboard.html          # لوحة التحكم
├── tensorrt.html          # TensorRT Integration
├── download.html          # صفحة التنزيل
├── splash.html            # Electron splash screen
│
├── css/
│   ├── style.css          # التنسيقات الرئيسية
│   └── dashboard.css      # تنسيقات لوحة التحكم
│
├── js/
│   ├── app.js             # الوظائف الرئيسية
│   ├── i18n.js            # نظام الترجمة
│   ├── interactive.js     # التفاعلات
│   └── dashboard.js       # لوحة التحكم
│
├── electron/
│   ├── main.js            # Electron main process
│   └── preload.js         # Electron preload script
│
├── manifest.json          # PWA manifest
├── service-worker.js      # PWA service worker
├── package.json           # تبعيات Node.js
└── README.md              # التوثيق
```

### إضافة ميزات جديدة:

#### 1. إضافة صفحة جديدة:
```html
<!-- new-page.html -->
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>صفحة جديدة</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <!-- محتوى الصفحة -->
    <script src="js/i18n.js"></script>
</body>
</html>
```

#### 2. إضافة ترجمات جديدة:
```javascript
// في js/i18n.js
translations.ar['new_key'] = 'النص العربي';
translations.en['new_key'] = 'English Text';
```

#### 3. تخصيص الألوان:
```css
/* في css/style.css */
:root {
    --primary-cyan: #00d9ff;    /* لون أساسي مخصص */
    --accent-blue: #0099ff;     /* لون ثانوي */
}
```

---

## ✨ الميزات

### 1. نظام اللغات المتعدد 🌐
- تبديل فوري بين العربية والإنجليزية
- حفظ تفضيلات المستخدم
- دعم RTL/LTR كامل

### 2. تطبيق سطح المكتب 🖥️
- بناء باستخدام Electron
- دعم Windows، macOS، Linux
- قوائم نظام محلية
- تكامل مع نظام التشغيل

### 3. تطبيق ويب تقدمي (PWA) 📱
- عمل دون اتصال
- تثبيت على الشاشة الرئيسية
- تحديثات تلقائية
- إشعارات Push

### 4. واجهة مستخدم متقدمة 🎨
- تصميم Cyberpunk عصري
- رسوم متحركة سلسة
- استجابة كاملة (Responsive)
- تأثيرات بصرية متقدمة

### 5. أداء محسّن ⚡
- تحميل سريع
- تخزين مؤقت ذكي
- معالجة GPU
- شريط تمرير محسّن

---

## 🐛 إصلاح المشاكل الشائعة

### المشكلة: Service Worker لا يعمل
```javascript
// تحقق من Console في المتصفح:
// يجب أن ترى: "✅ Service Worker registered"

// إذا لم يعمل، تأكد من:
1. استخدام HTTPS أو localhost
2. مسار service-worker.js صحيح
3. لا يوجد أخطاء في الكونسول
```

### المشكلة: Electron لا يبدأ
```bash
# حذف node_modules وإعادة التثبيت:
rm -rf node_modules
npm install

# تشغيل في وضع Debug:
npm run dev
```

### المشكلة: بناء Electron يفشل
```bash
# تثبيت electron-builder عالمياً:
npm install -g electron-builder

# تنظيف الـ cache:
npx electron-builder clean
npm run build
```

---

## 📝 الترخيص

MIT License - انظر ملف [LICENSE](LICENSE) للتفاصيل.

---

## 👨‍💻 المطور

**M.3R3** - [GitHub](https://github.com/QAZ83)

---

## 🙏 شكر خاص

- NVIDIA لـ CUDA و TensorRT
- Khronos Group لـ Vulkan
- المساهمون في مشاريع مفتوحة المصدر

---

## 📞 الدعم

- **Issues**: [GitHub Issues](https://github.com/QAZ83/ai-forge-studio-site/issues)
- **Discussions**: [GitHub Discussions](https://github.com/QAZ83/ai-forge-studio-site/discussions)
- **Email**: support@aiforgestudio.net

---

## 🚀 الخطوات التالية

- [ ] إضافة دعم TensorFlow
- [ ] دعم PyTorch
- [ ] محرر كود متقدم
- [ ] تكامل Git
- [ ] نظام Plugins

---

**تم التطوير بكل ❤️ و ⚡ بواسطة M.3R3**
